{
  "name": "ohanna-landing-page",
  "private": true,
  "scripts": {
    "build": "npm run build:api && npm run build:frontend",
    "build:api": "cd api-server && npm run build",
    "build:frontend": "cd ohanna && npm run build",
    "install:all": "npm ci && cd api-server && npm ci && cd ../ohanna && npm ci"
  },
  "dependencies": {}
}
