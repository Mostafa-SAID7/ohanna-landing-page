{
  "name": "ohanna-landing-page",
  "private": true,
  "scripts": {
    "build": "npm run build:api && npm run build:frontend",
    "build:api": "cd api-server && npm ci && npm run build",
    "build:frontend": "cd ohanna && npm ci && npm run build"
  },
  "dependencies": {}
}
